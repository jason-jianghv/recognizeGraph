---
description: 自动记录与同步产品计划进度（已完成 / 下一步）
alwaysApply: true
---

# 计划进度跟踪

真源文件：`docs/计划进度.md`（「下一步」+「已经完成」）。

## 何时写入「下一步」

用户提出**新计划 / 后续要做 / idea / 下一步**（即使只是随口一提）时：

1. 在 `docs/计划进度.md`「下一步」表**最上方**加一行，分配新 `P-xxx`（顺延最大编号）。
2. 状态默认 `待做`；说明写清范围与勿做边界。
3. 若影响产品范围：同步一行到 `PRD.md` §4；若影响页面清单：在 `screens.md` 加未勾选项。
4. 当日 `docs/工作记录.md` 留一句索引（含 ID）。

不要只口头答应「以后做」而不落文件。

## 何时同步「已经完成」

某计划或可独立交付的切片**在本会话落地**后：

1. 从「下一步」移到「已经完成」表最上方（或大计划拆成 `P-xxxa` 完成、余量仍留「下一步」）。
2. 写清完成日期与交付摘要（路径/接口/关键行为）。
3. 回写 `PRD.md` / `screens.md` 对应表述（「下一步」→「已做」或勾选）。
4. 工作记录「今日做了什么」点名计划 ID。

## 执行时

- 动手前先扫一眼 `docs/计划进度.md`「下一步」，优先用户点名的 ID。
- 开始做某项时把状态改为 `进行中`。
- 计划冲突仍遵守 `clarify-conflicts`：先问再做。

## 不要

- 不要用本文件替代工作记录里的踩坑/命令。
- 不要把未实现功能标成已完成。
- 不要新建第二套平行进度表；以本文件为准。
