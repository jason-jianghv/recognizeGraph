name: shitu_app
description: 识图 — 儿童拍照识物学习 App
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  http_parser: ^4.0.2
  image_picker: ^1.1.2
  path: ^1.9.0
  path_provider: ^2.1.5
  provider: ^6.1.2
  camera: ^0.12.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/splash-hero.png
    - assets/images/app-logo.png
