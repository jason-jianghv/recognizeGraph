---
description: 维护 docs/工作记录.md：进展、踩坑、决策与阶段里程碑
alwaysApply: true
---

# 工作记录维护

本项目使用单文件工作日志：`docs/工作记录.md`。

## 何时更新

在本仓库协作时，遇到下列情况应**及时写入**（不要攒到用户催才写）：

1. **完成一块可描述的工作**（页面、脚本、文档、联调）→ 写入当日「今日做了什么」
2. **踩坑并解决**（报错、推送失败、Figma/代理/权限等）→ 写入当日「踩坑与解决」，并在文首「踩坑速查」表**最上方**加一行：`症状 → 原因 → 解法`
3. **做出重要取舍**（方案 A/B、是否用 LaunchAgent 等）→ 「决策与取舍」
4. **阶段性完成**（MVP 一屏、仓库上线、可演示版本）→ 「阶段里程碑」表追加一行
5. **发现可复用命令** → 「常用命令备忘」

## 写法要求

- 用简体中文、短句、可检索关键词（保留报错原文关键片段）
- 不要只写「修好了」；写清**现象 / 原因 / 做法**
- 不要覆盖「当日自动摘要（Git）」小节（由 `scripts/daily-sync.sh` 维护）
- 若当天还没有 `### YYYY-MM-DD` 标题，先按文件内已有模板新增一日，再写内容

## 与定时任务的关系

每天 18:00 LaunchAgent 会在「当天有改动」时刷新 Git 自动摘要并 push。Cursor 负责写**可读经验**；定时任务负责**不丢提交事实**。
