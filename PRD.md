# 识图 App — 产品需求文档（PRD）

> 状态：MVP 可开发（与 Figma + 设计令牌配合使用）  
> 最后更新：2026-08-13  
> 说明：本文档是**产品与交互真源**；视觉样式以 Figma + `.cursor/skills/shitu-design-system/` 为准。目标粒度：**读完 PRD + 对照 Figma，即可实现页面与业务逻辑**，无需再口头补规则。未定内容用「待补充」标注。

---

## 0. 怎么用这份文档开发

| 角色 | 看什么 |
|------|--------|
| 产品 / 验收 | 第 1–2、5、8 节；第 6 节各页「状态表」 |
| 客户端开发 | 第 5–6 节全流程 + **第 6.11 场景判定** + 第 7 节接口约束 |
| 设计 / Figma | 第 6 节结构与文案；视觉令牌见设计 Skill，勿在 PRD 重复色值 |
| 后端开发 | 第 3、6.11、7 节；百度映射见 `docs/实施计划-百度识图.md` |

**冲突处理**：口头需求与本文矛盾时，以本文为准，或先改 PRD 再改代码/稿（见协作规则：冲突先问再做）。

---

## 1. 产品概述

### 1.1 一句话描述
面向小朋友的拍照识物学习 App：用手机摄像头拍下身边事物，识别后给出易懂的物体介绍，并配以百科式小短片，让孩子在玩耍中认识新事物。

### 1.2 要解决的问题
- 小朋友对身边事物充满好奇，但家长未必能随时、生动地讲清楚「这是什么、为什么」。
- 传统百科或卡片学习偏被动、与真实场景脱节，孩子难以把知识和眼前实物对应起来。
- 希望把「看到 → 提问 → 理解」变成一次简单的拍照动作，降低学习门槛、提高兴趣。

### 1.3 目标用户
- **主要使用者**：学龄前至小学低年级小朋友（在家长陪伴或指导下使用）。
- **购买 / 决策者**：家长、看护人（关注内容安全、学习效果与使用体验）。
- **使用场景倾向**：居家、户外散步、动物园/博物馆等「看见就想知道」的时刻。

### 1.4 核心价值 / 为什么做
- **即拍即学**：真实世界即教材，把好奇心转化为一次可完成的小学习。
- **识别 + 讲解一体**：接口返回物体信息，再用适合儿童的语言与百科小短片讲清楚，不只给一个名字。
- **寓教于乐**：以拍照探索为主路径，减少枯燥说教，培养观察与求知习惯。

---

## 2. 使用场景

| 场景 | 用户是谁 | 在什么情况下 | 想达成什么 | 优先级 |
|------|----------|--------------|------------|--------|
| 居家识物 | 学龄前～小学低年级孩子（家长可在旁） | 在家看到水果、家具、玩具等身边物品，好奇「这是什么」 | 拍照后立刻知道名称，并听到/看到适合儿童的简短介绍 | MVP |
| 户外散步认自然 | 孩子为主，家长陪伴 | 公园、小区散步时看到花草、虫子、车辆等，指着问「那是什么」 | 当场识别，再通过百科小短片大致了解「长什么样、常见在哪」 | MVP |
| 亲子共学 | 家长 + 孩子一起用 | 出门途中孩子不断发问，家长不便或无法讲清楚 | 一起拍照完成讲解，减轻家长负担，孩子当场搞懂 | MVP |
| 场馆探索 | 孩子 + 家长 | 动物园、博物馆等对着展品/动物想了解更多 | 得到适合儿童的讲解与小短片，把参观变成可带走的小学习 | 后续 |

---

## 3. 核心功能（MVP）

> 优先做「最小可用版本」：拍到图 → 多候选确认 → 详情学习 →（登录后）记入空间历史与等级。

### 3.1 拍照 / 选图

| 能力 | MVP 要求 |
|------|----------|
| 相机拍摄 | 全屏取景；关闭、闪光灯（设备不支持则提示）；可切换前后摄；**快门后进入对准裁剪页**（可缩小/平移后再确认） |
| 取景框 | 中央引导框（略收紧）+ 提示文案「把小伙伴放进框里再拍哦」 |
| 裁切 | **拍照/相册确认后**按取景框裁切再上传；结果页顶部图、空间历史缩略图均用裁后图 |
| 相册 | 选图 → **对准裁剪页**（平移 / 捏合 / 拉杆缩放，**可缩小至整图落入框内**）→「重选」或 ✓ 确定 → 裁切后识别；关闭回拍照页 |
| 分类 | 拍照前必选其一：动物 / 植物 / 交通工具与建筑（与探索三类一致） |
| 权限 | 首次相机/相册拒绝时，儿童友好说明 + 可再次引导 |

### 3.2 识别内容

| 项 | MVP 约定 |
|----|----------|
| 识别对象 | 仅三类：动物、植物、交通工具与建筑（不识别文字/文档/菜品等） |
| 引擎 | 百度智能云图像识别（经自有后端）；密钥只在服务端 |
| 结果形态 | 多候选列表：`name`、`score`（0～1）、`oneLiner`、`description`（百科长文）、`imageUrl`、`baikeUrl`、`id` |
| 候选数量 | 建议 Top 3～5（以后端归一化为准）；结果页**额外固定一页**「以上都不是」 |
| 置信度用途 | ① 卡片上展示「可信度 N%」；② 参与结果页顶栏文案分支（见 6.11.1） |

### 3.3 结果呈现与后续动作

- 结果页：**多候选横滑 + 顶栏联动文案** + 点卡片进详情；支持「以上都不是」反馈；返回关掉整条拍照链路；右上角「再拍」。
- 详情页：名称 + 介绍正文 +「你可能听过」+「听一听介绍」（**百度短文本 TTS**：播名字+简介；超限分段拼接）。
- 学习记次（需登录）：详情**停留满 5 秒**或点「听一听介绍」（先到先记，同页只记 1 次）；写入「我的本领宝藏箱」；更新等级。从未登录详情学习**不记次**。

### 3.4 账号 / 空间 / 历史

| 能力 | MVP 要求 |
|------|----------|
| 登录 | 大陆手机号 11 位 + 短信验证码；60s 倒计时；**默认不勾选**协议，未勾选点登录弹窗「同意并登录 / 取消」；可点开《用户协议》《隐私政策》 |
| 未登录空间 | 仅引导「去登录」；无历史、无设置 |
| 已登录空间 | 头像、昵称、已学次数、Lv、副文案「再学 X 次就到 Lv N」；「我的本领宝藏箱」按月 |
| 历史列表 | 仅展示**有记录的月份**（空月不出现）；月内一行 3 卡；**名称始终显示**（勿「按住显名」） |
| 历史进详情 | 展示与识别链路一致的百科介绍（落库 `description`）；**不重复记次** |
| 设置 | 关于我、版本号、退出登录 |

### 3.5 等级（与学习次数绑定）

- 等级由累计「有效学习次数」决定，最高 **Lv20**。
- 门槛表（累计次数 ≥ 该值即达到该级）：  
  `0,1,3,6,10,16,24,34,48,66,90,120,158,206,266,341,436,556,706,896`  
  （对应 Lv1～Lv20；前后端须一致，见 `app/lib/utils/level_rules.dart` / `server/app/auth/levels.py`）
- 空间副文案：未满级 →「再学 X 次就到 Lv N」；满级 →「已是最高等级啦」。

---

## 4. 非 MVP / 后续功能

> **计划进度真源**：[`docs/计划进度.md`](docs/计划进度.md)（「下一步」/「已经完成」）。下表为产品说明摘要；增删计划以进度文件为准并回写本表。

| 功能 | 说明 |
|------|------|
| 真实短信下发 | MVP 可本地生成验证码并 Toast 展示；正式环境接短信通道后去掉明文码 |
| TTS 真人播报增强（精品音库/拖动精细定位） | 已接百度短文本非流式；可后续换童声音色 |
| 百科短片 / CMS | 「你可能听过」目前可用占位文案（计划 P-002） |
| 儿童向口语改写（LLM） | MVP 先用百度百科 description（计划 P-001） |
| 勋章墙 | 空间可后置 |
| **探索/更多接真实物种库** | **已完成 P-005 / P-008 / P-011**：常规种子（seed）进探索；tropicals 生僻留库不删、默认不展示；识别回填需图+简介；缺简介可维基补齐 |
| **学习排行榜** | 计划 **P-015**：独立页；探索首页列表第一位入口（分类前）；总榜 + 周榜；按学习次数排前 20；页底当前用户名次 |
| **作业批改 Tab** | 计划 **P-016**：底栏新增 Tab；拍照调服务返回批改结果；服务商与结果页交互待定 |
| **社区讨论（学习心得分享）** | 计划 **P-003**：发帖分享学习心得；儿童审核与安全单独设计；入口勿挤占探索/拍照 |
| 服务器缓存 TTS MP3 | 计划 **P-004**：见 `docs/tts-音频缓存.md` §4 |
| 场馆专属模式、离线识别、付费 | 待定 |
| 鸿蒙 | 平台后置；MVP 优先 iOS / Android（Flutter） |

> **物种目录备忘**：库表 `catalog_species`（与用户 `learning_records` 解耦）。识别回填 `source=recognize`；下一步种子数据 `source=seed`。同一分类下同名只一行，`seen_count` / `best_score` 累积。

> **排行榜备忘（P-015）**：指标为「学习次数」（与空间已学次数同源）。榜单展示前 20；页底固定区展示「我的名次」（超出前 20 也显示名次；未登录引导登录）。总榜 / 周榜切换；周榜统计窗口待实现时定（自然周或滚动 7 日）。

> **作业批改备忘（P-016）**：新增底栏 Tab，与探索 / 空间并列；主路径为拍照（或相册）→ 上传后端 → 批改服务 → 展示结果。与识物拍照流程解耦（独立入口与结果页）；儿童向文案与隐私（作业图是否落盘）实现前再定。服务商待选型。

> **社区讨论备忘（idea，未排期）**：目标是把「拍完就走」延伸成「学完可分享」——例如针对某次识别写一句心得、晒宝藏箱某条，或按话题（动物/植物…）浏览他人短帖。实现前先定：发帖是否实名/昵称、是否允许配图、点赞/评论范围、内容审核与举报、是否仅登录可见。

---

## 5. 用户流程

### 5.1 整体导航结构

```
底部导航
├── Tab「探索」…… 推荐百科内容（默认首页）；列表首项可进「学习排行榜」（P-015，待做）
├── Tab「作业批改」…… 拍照批改作业（P-016，待做）
├── Tab「空间」…… 用户信息 / 学习进度 / 我的本领宝藏箱
└── 拍照按钮（右侧独立入口）→ 全屏拍照识物页（与作业批改入口分开）
```

冷启动：开屏页（短暂）→ 探索 Tab。  
说明：作业批改 Tab 为后续计划，落地前底栏仍为「探索 + 空间 + 拍照」。

### 5.2 主路径：拍照识物学习

```
任意 Tab 点击底部拍照按钮
  → 拍照页（权限引导；选分类；取景或相册裁剪）
  → 上传裁后图 + category → 识别中
  → 识别结果页（多候选 +「以上都不是」；顶栏按置信度分支文案）
       → 点候选卡 → 详情页（百科介绍；可记学习）
       → 点「以上都不是」→ 上报反馈 → 感谢弹窗（可再拍）
  → 结果页返回：关结果 + 关拍照，回原 Tab
  → 结果页再拍：回拍照页
  → 识别失败：友好提示 + 再拍 / 换图
```

步骤要点：

1. 默认进「探索」。
2. 点拍照 → 选分类 → 对准取景框拍摄，或相册对准裁剪。
3. 识别返回多候选；结果页让用户确认，而非直接认定唯一答案。
4. 点候选进详情学习；登录用户停留满 5 秒 / 听一听 → 记历史 + 等级。
5. 「以上都不是」不进详情，只上报反馈。

### 5.3 路径：从推荐内容学习

```
探索 → 三类推荐横滑卡片 → 点卡片进详情
     → 或点「更多 ›」/ 横滑末尾续滑 → 二级列表 → 点行进详情
详情学习（登录则记次 / 写历史，规则同 6.11.3）
```

### 5.4 路径：查看个人空间

```
空间
  ├─ 未登录 →「去登录」→ 登录页 → 成功后已登录空间
  └─ 已登录 → 概览 + 按月历史 → 点卡片进详情（source=history，不记次）
       → 设置 → 关于我 / 退出登录
```

---

## 6. 界面与交互

### 6.1 页面一览

| 页面 | 主要做什么 | 备注 |
|------|------------|------|
| 探索（Tab） | 推荐百科，引导浏览学习 | 默认首页；三类分区 + 更多；列表首项→排行榜（P-015） |
| 更多（二级页） | 某类完整推荐列表 | 下拉刷新 / 上滑加载更多 |
| 学习排行榜（二级页） | 总榜 / 周榜；前 20 + 我的名次 | 计划 **P-015**，待做 |
| 作业批改（Tab） | 拍照上传 → 展示批改结果 | 计划 **P-016**，待做 |
| 空间（Tab） | 用户信息、进度、我的本领宝藏箱 | 分未登录 / 已登录 |
| 拍照页 | 取景或进相册裁剪，发起识别 | 全屏；识物主路径 |
| 相册对准裁剪页 | 平移/缩放主体进框后确认 | 从相册进入 |
| 识别结果页 | 多候选确认 + 低置信文案 +「以上都不是」 | 识别非 100% 准 |
| 详情页 | 名称、介绍、「你可能听过」、听一听 | 多入口，结构一致 |
| 登录 / 设置 / 关于我 / 开屏 | 账号与品牌 | 见下各节 |

视觉布局、组件尺寸以 **Figma** 为准；下述为结构与逻辑约束。

### 6.2 底部导航

- 左侧胶囊 Tab：**探索**、**空间**；当前高亮。（后续 **P-016** 增加 **作业批改** Tab）
- 右侧独立圆形**拍照**按钮；任意 Tab 可进拍照（识物主路径，与作业批改入口分开）。
- 拍照为高频主操作，视觉上与 Tab 区分开。

### 6.3 探索页

- 标题「探索世界」。
- 引导文案示意：「举起小小的镜头，框住大大的好奇……」；副文案示意：「今天想认识谁？点开小卡片学一学~」。
- **列表第一位（分类区之前）**：学习排行榜入口（计划 **P-015**，待做）→ 进入排行榜页。
- 三个分类区：动物 / 植物 / 交通工具与建筑；区标题 + 右上「更多 ›」。
- 卡片：图 + 名称（单行省略居中）+ 描述；点击进详情。
- 横滑列表末尾：「继续滑动展示更多」提示卡；**继续滑过一段自动进入该类二级列表**（也可点提示卡或「更多 ›」）。

### 6.3a 学习排行榜（计划 P-015，待做）

- 独立二级页；从探索列表首项进入。
- **总榜** / **周榜** 切换；按学习次数排序，展示前 **20** 名（昵称 / 头像 / 次数等，具体 UI 以 Figma 为准）。
- 页面下方固定展示**当前用户名次**（即使未进前 20）；未登录引导登录后再看本人名次。

### 6.3b 作业批改（计划 P-016，待做）

- 底栏独立 Tab；主操作：拍照或相册 → 上传后端 → 批改服务 → 结果页。
- 与识物「拍照按钮」流程解耦；结果展示形态（对错标注 / 评语等）随所选服务再定。

### 6.4 空间页

#### 未登录

- 无用户信息、无历史、无设置。
- 默认头像占位 + 引导文案 + 主按钮**去登录**。
- 底栏 Tab / 拍照仍可用。

#### 登录页

- 关闭回上一页；大标题「登录识图」；副文案「手机号验证一下，就能保存你的小天地啦」。
- 手机号（11 位）、验证码 +「获取验证码」（60s 倒计时，倒计时中不可重复点）。
- 主按钮「登录」。
- **协议勾选**（默认**不勾选**）：勾选框 + 文案「登录即表示同意《用户协议》和《隐私政策》」；《用户协议》《隐私政策》可点击，进入应用内全文页。
- **未勾选点登录**：弹窗提示需先同意；按钮「同意并登录」「取消」。
  - 「取消」：关弹窗，不登录。
  - 「同意并登录」：勾选协议并关弹窗；若已填验证码则继续登录；若未填验证码 → Toast「还没有填入验证码」。
- 已勾选点登录：走正常登录（缺验证码仍 Toast「请输入验证码」）。
- 成功 → 已登录空间；失败 Toast 说明原因。

#### 已登录

- 标题「空间」；右上**设置**。
- 头像、昵称、已学习次数、Lv 胶囊；副文案见 3.5。
- **我的本领宝藏箱**（原「拍照历史」/「我的时光宝藏图」）：强调「学到的本领」；月份倒序（跳过空月）；月内 3 列网格；卡底**始终**叠名称（白字 + 半透明底）；**卡片图为用户拍摄缩略图**（非接口百科图）。
- 点击卡片 → 详情（顶图仍用接口百科图；须带上已存介绍文案，见 6.11.3）。
- **自动刷新**：切回「空间」Tab，或详情记学习导致已学次数变化时，列表自动重新拉取（可静默更新，不必整页 Loading）。

#### 设置 / 关于我

- 设置：关于我、版本号（如 v1.0.0）、底部退出登录。
- 关于我：图标、名称「识图」、版本、儿童向产品介绍；返回设置。

### 6.5 拍照页

- 上：取景预览；下：白底操作区。
- 顶：关闭、闪光灯。
- 中：取景框 +「把小伙伴放进框里再拍哦」。
- 分类胶囊：动物 / 植物 / 交通工具与建筑（识别前可切换）。
- 底：左相册 | 中快门 | 右翻转摄像头。
- **快门**：拍完 → **对准裁剪页**（可缩小/平移）→ ✓ 裁切 → 识别中 → 结果页。
- **相册**：选图 → 对准裁剪页 → 裁切 → 识别中 → 结果页。
- 失败示意文案：「没看清哦，再拍一张试试～」。

#### 相册对准裁剪页（逻辑）

| 操作 | 行为 |
|------|------|
| 平移 / 捏合 / 拉杆 | 调整图片，使主体进入取景框；**可缩小到整图落入框内**（不限于 ≥1x 铺满） |
| ✓ 确定 | 按与拍照页一致的取景几何裁切，进入识别 |
| 重选 | 回系统相册再选（拍照快门进入时可为「重拍」语义，当前仍关裁剪回拍照） |
| 关闭（X） | 放弃本次裁剪，回拍照页 |

### 6.6 识别结果页

> 核心是「展示候选、让用户确认」，不是直接认定唯一答案。

#### 页面结构

| 区域 | 内容 |
|------|------|
| 左上 | **返回**：关结果页 + 关拍照页 → 进入拍照前的 Tab |
| 右上 | **拍照 / 再拍**：回拍照页，可再拍或重选相册 |
| 顶部用户图 | 本次**裁后**用户图 |
| 顶栏标题 + 副文案 | **随当前横滑页联动**，且受「最高置信度」分支控制（见下表） |
| 横滑区 | 接口候选卡（若干）+ **最后一页固定「以上都不是」** |
| 底提示 | 候选页：「左右滑动看看其他可能，点卡片了解更多」；「以上都不是」页：「点卡片上报「认错了」，帮我们改进识别」 |

#### 候选卡片 UI（逻辑）

- 配图：**仅接口返回的百科/识别图 `imageUrl`**；无效或加载失败 → App Logo（**不**回退用户拍摄图）。
- 需过滤明显无意义的百度 Logo 类占位图（当作无图）。
- 图上叠加半透明圆角条：**可信度 N%**（`score × 100`，取整，夹在 0～100）。
- 当前选中卡描边加粗/主色；点候选卡 → 详情（「以上都不是」除外）。

#### 顶栏文案分支（必实现）

设 `maxScore` = 本轮全部候选 `score` 的最大值（无候选视为低置信）。

| 条件 | 顶栏标题 | 顶栏副文案 |
|------|----------|------------|
| 当前滑到「以上都不是」 | 以上都不是 | 点卡片告诉我们，帮识图认得更准～ |
| 否则且 `maxScore < 0.15`（即最高可信度 **&lt; 15%**） | **我居然被难住了** | **它可能是「{当前候选 name}」** |
| 否则（`maxScore ≥ 0.15`） | 当前候选 **name** | 当前候选 **oneLiner**（一句话简介） |

说明：

- 低置信时，**横滑切换候选仍会改变副文案里的名字**，标题保持「我居然被难住了」。
- 阈值 **15%** 为产品定数（`score < 0.15`）；改阈值须先改 PRD。
- 名称 + 一句话简介是「正常置信」时的默认形态；低置信用「被难住了」降低错误认定感。

#### 「以上都不是」

- 卡片背景用**用户裁后图** + 遮罩 CTA（文案如「以上都不是」/ 已反馈后「已收到反馈」）。
- 点击：调用反馈接口（带上本次展示过的候选与可选原图）；成功弹窗感谢；可「再拍一张」或「先这样」。
- 已反馈过再次点击：提示「已经收到啦」。
- **不进入详情页**，不记学习次数。

### 6.7 详情页

> 入口：识别候选 / 探索卡片 / 更多列表 / 空间历史。页面骨架一致，**文案与记次规则因入口而异**。

#### 结构

| 区域 | 内容 |
|------|------|
| 左上 | 返回上一页（从结果进则回结果，**不**因此关掉整条拍照链） |
| 顶图 | **接口返回图**（百科/识别配图）；无图时占位。用户拍摄图不作为详情顶图 |
| 正文 | **名称**（右侧点读喇叭）→ **英文名**（有则展示）→ **介绍** →（有百科链接时）**了解更多 点击我吧** →「你可能听过」→ 延伸文案 |
| 底栏 | 未播报：主按钮「听一听介绍」；播报中：播放/暂停 + 进度条（TTS 后置时可用 Toast 占位） |

#### 百科外链

- 文案固定：**了解更多 点击我吧**；浅桃胶囊底 + 书本图标 + 外链小箭头，更易点。
- 位置：简介正文正下方、「你可能听过」上方。
- 仅当接口/落库有 `baikeUrl`（非空）时展示；点击用系统浏览器打开该链接。
- 无链接时不展示入口（探索本地内容可无）。

#### 配图分工（全局约定）

| 位置 | 显示哪张图 |
|------|------------|
| 识别结果页顶部 | 用户拍摄/裁后图 |
| 识别结果页鉴定卡片 | 接口返回图（失败 → Logo） |
| 详情页顶部 | 接口返回图 |
| 我的本领宝藏箱卡片 | 用户拍摄缩略图（记学习时上传的 thumb） |

#### 文案填充规则（MVP）

| 入口 | 名称下方「介绍」 | 「你可能听过」 |
|------|------------------|---------------|
| 识别结果候选 | 优先百科 `description`；空则 `oneLiner`；再空则「可能是「{name}」」 | 占位：「关于「{name}」，我们以后会讲更多……」 |
| 探索 / 更多 | 推荐内容自带简介 / 描述 | 同结构；有 CMS 后接真实延伸 |
| 空间历史 | 使用记学习时落库的介绍正文；若旧数据为空，可降级「你认识过的小伙伴」 | 与识别入口相同的占位句式 |

#### 语音（MVP）

- 无分享、无评论。
- **引擎**：百度智能云短文本在线合成，**实时、非流式**；播报稿 = 名字 + 简介（`oneLiner`）。
- **超限**：单段超过短文本硬上限（1024 GBK 字节）时，服务端按标点**分段合成**，再**拼接为一条 MP3** 返回 App。
- **语音偏好**（本地持久化，默认 **A**）：
  | 代号 | 展示名 | 语速 | 情绪 | 音量/音调 | 百度音色（实现映射） |
  |------|--------|------|------|-----------|----------------------|
  | A | 小朋友（约3岁） | 偏慢 | 活泼 | 中等 | 度小童 + happy（不支持则降级无情绪） |
  | B | 男老师 | 中等 | — | 中等 | 度博文 |
  | C | 女老师 | 中等 | — | 中等 | 度小美 |
- **入口**：设置 →「语音偏好修改」；详情页右上角喇叭入口；「听一听介绍」下方提示「可在设置里改语音类型 · 去修改」。
- 点「听一听介绍」：请求 `POST /v1/tts`（带 `voice_profile`）→ 底栏播放/暂停 + 进度条；并仍触发记学习（若满足登录等条件）。**同页同偏好只合成一次**（本地临时 MP3）。
- **名字点读（中英）**：名称右侧喇叭；名称下展示英文（有则显示）。点播稿 = `中文名。English。`（方案 C，暂只英语）。
  - 英文来源：本地词表优先（约 1000+ 动物 / 1000+ 植物，见 `server/resources/name_en/`）→ 未命中再走百度机器翻译兜底。
  - **不**触发记学习；与底栏「听一听介绍」音频缓存分开；**同页 + 同偏好 + 同播报稿只合成一次**。
  - API：`GET/POST /v1/name-en`；朗读仍走 `POST /v1/tts`（`text` 传完整播报稿）。
  - 生成/缓存依据与后续「服务端缓存 MP3」方案见 `docs/tts-音频缓存.md`。
- 密钥只在服务端；控制台需开通语音合成；机翻兜底需开通**文本翻译**（与识图同 Key 的智能云 MT）。

### 6.8 交互要点（全局）

- **权限**：拒绝对话儿童可读；提供再开入口。
- **加载**：识别中友好等待；超时可取消/重试。
- **失败**：网络/识别失败 → 温和文案 + 再拍/换图。
- **多候选**：必须可滑动与点选。
- **安全**：结果与推荐适龄可控；密钥不下发 App。

### 6.9 更多（二级页）

- 返回探索；标题对应该类（动物百科 / 植物百科 / 交通与建筑）。
- 竖向列表：图、标题、描述、箭头；下拉刷新、上滑加载更多。
- 点行 → 详情。

### 6.10 开屏页

- 冷启动短暂展示 → 探索。
- 上半插画；下半背景偏米白；Logo「识」+「欢迎来到识图，认识世界从这里开始」。
- 无按钮；停留时长 / 是否可跳过实现时可定。

### 6.11 场景判定与业务规则（开发对照）

> 本节是易遗漏的「if-else」真源；实现与验收按表执行。

#### 6.11.1 结果页顶栏（置信度）

```
IF 当前页 == 以上都不是:
  标题 = 「以上都不是」
  副文案 = 「点卡片告诉我们，帮识图认得更准～」
ELSE IF max(候选.score) < 0.15:
  标题 = 「我居然被难住了」
  副文案 = 「它可能是「」+ 当前候选.name + 「」」
ELSE:
  标题 = 当前候选.name
  副文案 = 当前候选.oneLiner
```

#### 6.11.2 记一次「学习」的条件

同时满足才 +1 并写历史：

1. 用户已登录（有效 token）；
2. 详情 `source ≠ history`（从空间历史再次打开**不**记次）；
3. 本页尚未记过（**停留满 5 秒**与「听一听」**同一详情会话只记 1 次**，先到先记）；
4. 触发方式之一：进入详情后连续停留 **5 秒**，或点击「听一听介绍」。

说明：离开详情页会取消未触发的停留计时；5 秒内点「听一听」立即记次并取消计时。不再要求滚动到底。

写入字段至少包括：`name`、`category`、缩略图（用户图优先）、百科图 URL、**介绍正文（详情名称下方那段）**、score、candidateId、baikeUrl、source。  
空间历史再进详情时必须能还原介绍正文。

#### 6.11.3 未登录 vs 已登录

| 行为 | 未登录 | 已登录 |
|------|--------|--------|
| 探索 / 拍照 / 看结果 / 看详情 | ✅ | ✅ |
| 空间看历史 / 设置 | ❌（引导登录） | ✅ |
| 详情记学习 / 涨等级 | ❌（静默不记） | ✅ |
| 历史进详情看介绍 | — | ✅（依赖落库介绍） |

#### 6.11.4 导航关闭范围

| 操作 | 关闭范围 |
|------|----------|
| 结果页「返回」 | 结果页 + 拍照页（含裁剪若仍在栈上按实现收干净）→ 原 Tab |
| 结果页「再拍」 | 替换/回到拍照页，可新开识别 |
| 详情「返回」 | 仅详情 → 上一页（结果或列表） |
| 「以上都不是」再拍 | 回拍照页 |

#### 6.11.5 识别失败 / 空候选

- 接口错误、超时、无可用候选：不进入「假装很准」的结果；Toast/弹层友好提示，引导再拍或换图。
- 若仍进入结果且候选为空：按低置信处理；「以上都不是」仍可展示（产品可验收时确认是否允许空列表 + 仅反馈页）。

#### 6.11.6 登录验证码与协议

- 手机号：大陆 11 位数字校验。
- 获取验证码成功 → 60s 倒计时；倒计时期间按钮展示剩余秒数且不可点。
- MVP 演示：接口可返回 `code`，客户端 Toast「验证码：xxxxxx（演示用…）」；正式版删除明文展示。
- 协议勾选默认关闭；未勾选点登录必须弹窗，不得静默登录。
- 「同意并登录」且验证码为空：关弹窗 + Toast「还没有填入验证码」（不发起登录请求）。
- 《用户协议》《隐私政策》正文见应用内页面（`LegalTexts`）；正式上架前建议法务审阅。

---

## 7. 技术与约束

### 7.1 平台
MVP：iOS / Android（Flutter）。鸿蒙后置。

### 7.2 识别能力来源

**已定：百度智能云 · 图像识别（主识别引擎）**

| 用户分类 | 调用接口（MVP） | 说明 |
|----------|-----------------|------|
| 动物 | 动物识别 | 多候选 + score，可开 `baike_num` 取百科 |
| 植物 | 植物识别 | 同上 |
| 交通工具与建筑 | 通用物体和场景（主）+ 车型（交通兜底，可选） | 建筑/场景靠通用 |

**工程原则**

1. API Key / Secret Key **只放服务端**，App 不内置密钥。  
2. App 上传图片 + category → 自有后端 → 百度 → 归一化 `candidates[]`。  
3. 儿童向改写、短片 **后置**；MVP 百科文案 + **百度短文本 TTS**（名字+简介）。  
4. 详细实施见 `docs/实施计划-百度识图.md`。

### 7.3 隐私与数据
- 识别经自有后端；密钥与日志脱敏。  
- 登录会话 token 本地持久化；退出登录清除。  
- 学习历史含缩略图文件，随用户账号存储。  
- 用户协议 / 隐私政策：应用内可查阅全文（MVP 文稿）；正式环境建议法务审定后替换。
- 儿童场景：最小必要、家长知情原则。

### 7.4 性能与体验预期
- 端到端识别展示目标：约 3～5s（视网络）；失败可重试。  
- MVP 不要求离线识别。  
- 真机联调须保证 App 内 API Base URL 与电脑局域网 IP 一致（见工作记录踩坑）。

---

## 8. 成功标准（MVP 验收草案）

| 指标 / 用例 | 通过标准 |
|-------------|----------|
| 主路径 | 选分类 → 拍/裁 → 多候选结果 → 进详情看到介绍 |
| 低置信文案 | 人为低分或选难图时，最高分 &lt;15% → 标题「我居然被难住了」，副文案「它可能是「xxx」」 |
| 正常置信 | 最高分 ≥15% → 标题为名字，副文案为一句话简介 |
| 以上都不是 | 末卡可反馈成功，不进详情、不记次 |
| 学习闭环 | 登录后详情停留满 5 秒（或听一听），空间历史出现该条且次数/等级更新 |
| 历史详情 | 新记条目从历史进入，名称下介绍与识别进详情一致 |
| 历史不重复记 | 历史再进详情停留/听一听，次数不增加 |
| 未登录 | 可完整拍照学习浏览；空间仅引导登录；不写历史 |
| 裁切 | 结果顶图主体与取景框一致，无明显整框外背景主导 |

---

## 9. 开放问题 / 待决策

- [x] 识别对象？→ **动物 / 植物 / 交通工具与建筑**（已定）
- [x] 结果后主动作？→ **多候选确认 → 详情学习**（已定）
- [x] 是否需要账号？→ **手机号验证码；空间/历史/等级依赖登录**（已定）
- [ ] 免费 / 付费怎么定？
- [ ] 空候选时是否仍展示仅含「以上都不是」的结果页？
- [ ] 开屏停留时长与是否可跳过
- [x] 隐私政策 / 用户协议正文（MVP 应用内文稿；上架前法务再审）
- [ ] 「你可能听过」正式内容来源（CMS / 运营 / 模型）

---

## 10. 构思笔记（随便写）

> 想到什么就往下记，不用整齐。产品定案后应上收到第 3 / 6.11 节，避免只留在笔记里。

- 低置信用「我居然被难住了」是为了避免低分时仍用肯定语气报名字。
- 历史必须落库介绍，否则空间回看详情会「空介绍」。
- 结果页名称/简介主要在**顶栏联动**；卡片内以图 + 可信度为主（以 Figma 为准）。
